learning_rate: 2e-5
weight_decay: 0.01
warmup_steps: 0
max_epochs: 1
patience: 3
mlflow_experiment: message_classification
cpu_threads: 20
log_model: true  # Whether to log model to MLflow (disable in tests for speed)
